# Email Configuration (Required for lead notifications)
# Use Gmail App Password for EMAIL_PASS (not your regular password)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password

# Admin Configuration
ADMIN_EMAIL=your-admin-email@gmail.com

# Security (Required)
# Generate a random string for SESSION_SECRET
SESSION_SECRET=your-random-session-secret-here

# Database (Optional - Vercel will handle this)
DATABASE_URL=your-database-url-if-using-external-db

# Port (Optional - Vercel will handle this)
PORT=8080

# ==========================================
# SETUP INSTRUCTIONS:
# ==========================================
# 
# 1. EMAIL_USER: Your Gmail address
# 2. EMAIL_PASS: Gmail App Password (NOT your regular password)
#    - Go to Google Account Settings > Security > 2-Step Verification
#    - Generate App Password for "Mail"
# 3. ADMIN_EMAIL: Email where you want to receive lead notifications
# 4. SESSION_SECRET: Any random string (e.g., "my-roofing-app-secret-2024")
# 5. DATABASE_URL: Leave empty for Vercel (they'll handle it)
# 6. PORT: Leave as 8080 (Vercel will override this)
#
# ==========================================
# GMAIL APP PASSWORD SETUP:
# ==========================================
# 1. Go to https://myaccount.google.com/
# 2. Security > 2-Step Verification (enable if not already)
# 3. Security > App passwords
# 4. Select "Mail" and generate password
# 5. Use that 16-character password as EMAIL_PASS
#
# ========================================== 